# ── Node / frontend ───────────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
.vite/
vite.config.ts.timestamp-*
# machine-local pnpm overrides (e.g. host-webapp-pi.sh's exFAT store-dir)
.npmrc

# ── Packaging output ──────────────────────────────────────────────
*.deb
*.rpm
*.flatpak
packaging/flatpak/.flatpak-builder/
packaging/flatpak/build/
packaging/flatpak/repo/
packaging/flatpak/staging/
.flatpak-builder/
packaging/flatpak/io.github.rubix_coder.LetsGoApp.metainfo.xml

# ── Tooling / scratch ─────────────────────────────────────────────
# graphify knowledge-graph output (regenerated by the /graphify skill)
graphify-out/
# codegraph symbol index (regenerated by `codegraph index`/`codegraph sync`)
.codegraph/
# dev-loop orchestration scratch
.dev-loop/
# Aider local session history (analogous to shell history — not repo content)
.aider.chat.history.md
.aider.input.history
# stray render/scratch output at repo root
/output.pdf

# ── Editor / OS ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/*
!.vscode/extensions.json

# ── Logs / local secrets ──────────────────────────────────────────
*.log
.env
.env.*
!.env.example

# ── Personal data — NEVER commit ──────────────────────────────────────
# Exported vaults contain real tasks/notes AND credentials (WebDAV password,
# Google OAuth client id, email addresses). They are backups, not repo content.
letsgo-export-*.json
*-export-*.json
vault-backup*.json
# Scratch space for personal notes/specs kept beside the code.
private/
docs/private/

# Local editor workspace
*.code-workspace

# AI-assistant working files
CLAUDE.md
.claude/
.aider*
